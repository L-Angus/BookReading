#ifndef __ALLOCATOR_HPP__
#define __ALLOCATOR_HPP__

#include <memory>
#include <string>

// void Test_Allocator() {
//   std::allocator<std::string> alloc;
//   auto const p = alloc.allocate(10);
//   auto q = p;
//   // 在 C++17 之后，construct 被废弃，使用 std::allocator_traits 的 construct
//   // 替代
//   alloc.construct(q, "hi"); // C++17 之前的用法
// }

#endif